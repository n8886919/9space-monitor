name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - name: Validate
        run: |
          set -euo pipefail
          python -m compileall -q custom_components/nine_space_nvr_monitor
          python -m unittest discover -s tests -v

      - name: Build installation archive
        run: |
          set -euo pipefail
          mkdir -p dist/custom_components
          cp -R \
            custom_components/nine_space_nvr_monitor \
            dist/custom_components/
          (
            cd dist
            zip -qr ../nine_space_nvr_monitor.zip custom_components \
              -x "*/__pycache__/*" "*.pyc"
          )
          sha256sum nine_space_nvr_monitor.zip \
            > nine_space_nvr_monitor.zip.sha256

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh release create "$GITHUB_REF_NAME" \
            nine_space_nvr_monitor.zip \
            nine_space_nvr_monitor.zip.sha256 \
            --generate-notes \
            --title "$GITHUB_REF_NAME"
