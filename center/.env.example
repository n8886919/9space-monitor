# Keep localhost unless this host firewall/Tailscale ACL restricts access.
# For a Tailnet-only listener, replace this placeholder locally with that
# machine's Tailscale address. Never commit a real site address.
CENTER_BIND_ADDRESS=127.0.0.1
